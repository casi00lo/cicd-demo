{
  "name": "docker_web_app",
  "version": "1.0.0",
  "description": "Node.js on Docker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "npm install inherits && mocha",
    "dev": "docker-compose up -d --port 3000 start"
  },
  "dependencies": {
    "@instana/collector": "^1.137.0",
    "express": "^4.16.1"
  }
}
